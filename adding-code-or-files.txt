Adding files:

  git add <file>
  git commit -m "added <file> blah blah blah"

Updating file:

  <change file>
  git commit -m "changed <file> blah blah blah"

Pushing changes to github so everybody can see them:

  git push origin master

Update local repo with changes from github:

  git pull
